class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:

        if len(list(set(nums))) != len(nums):  #  Set only stores a value once even if it is inserted more than once

            return True

        return False
