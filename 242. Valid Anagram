class Solution:
    def isAnagram(self, s: str, t: str) -> bool:

        if len(s) != len(t):

            return False

        s_sort, t_sort = sorted(s), sorted(t)

        for char in range(len(s_sort)):

            if s_sort[char] != t_sort[char]:

                return False

        return True 
        
